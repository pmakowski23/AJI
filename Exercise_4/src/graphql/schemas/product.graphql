type Product @entity {
  id: ID! @id
  name: String! @column
  description: String! @column
  price: Float! @column
  weight: Int! @column
  category: Category! @link
}

input UpdateProductInput {
  name: String
  description: String
  price: Float
  weight: Int
  category: Category
}
